文件所在位置：      resource\resource\filePathExplain.txt

打包之后的文件位置： BOOT-INF\classes\resource\filePathExplain.txt

是否可以直接访问：   否

是否可以访问：      是

访问地址：         http://localhost:8090/zty/resource/filePathExplain.txt

需要配置：         是

具体配置：         静态资源访问设置了 registry.addResourceHandler("/**").addResourceLocations("classpath:/");
                                      使得访问/static时实际访问的文件是classpath:/下的文件

是否会被忽略：      无

忽略配置(示例)：